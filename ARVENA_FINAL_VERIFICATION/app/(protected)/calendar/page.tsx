import { ResourceList } from "@/components/resource-list";import { CommandForm } from "@/components/command-form";export default function Page(){return <><main className="page"><div className="page-head"><div><h1>Calendar</h1><p>Visit adalah scheduling source of truth.</p></div></div><CommandForm command="schedule_visit" fields={[{name:'p_job',label:'Job ID',required:true},{name:'p_visit',label:'Visit ID (blank=create)'},{name:'p_start',label:'Start UTC',type:'datetime-local',required:true},{name:'p_end',label:'End UTC',type:'datetime-local',required:true},{name:'p_timezone',label:'IANA timezone',required:true},{name:'p_address',label:'Address snapshot JSON',placeholder:'{}'},{name:'p_assign_members',label:'Member IDs comma-separated'},{name:'p_override',label:'Override conflict?',placeholder:'false'},{name:'p_reason',label:'Override reason'},{name:'p_expected_version',label:'Expected version',type:'number'}]}/></main><ResourceList resource="visits"/></>}
