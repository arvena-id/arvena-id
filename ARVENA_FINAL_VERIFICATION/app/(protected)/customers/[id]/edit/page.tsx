import { EntityDetail } from "@/components/entity-detail";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <main className="page"><div className="notice notice-warning">Customer edit command belum memiliki RPC hardened yang dapat diverifikasi pada migration chain saat ini. Record tetap ditampilkan read-only agar tidak melakukan unsafe direct mutable write.</div><EntityDetail entity="customers" id={id}/></main>}
