import { CommandForm } from "@/components/command-form";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <main className="page"><div className="page-head"><div><h1>Edit Job</h1><p>Scheduling fields tidak ditulis langsung ke Job.</p></div></div><CommandForm command="update_job_fields" fields={[{name:'p_job',label:'Job ID'},{name:'p_expected_version',label:'Expected version',type:'number'},{name:'p_title',label:'Title'},{name:'p_description',label:'Description'},{name:'p_priority',label:'Priority'},{name:'p_estimated',label:'Estimated minor',type:'number'}]} defaults={{p_job:id}} onSuccess={`/jobs/${id}`}/></main>}
