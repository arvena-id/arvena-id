import { EntityDetail } from "@/components/entity-detail";import { ResourceList } from "@/components/resource-list";import { CommandForm } from "@/components/command-form";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><EntityDetail entity="jobs" id={id} editHref={`/jobs/${id}/edit`}/><main className="page"><h2>Job actions</h2><div className="grid-3"><CommandForm command="confirm_job" fields={[{name:'p_job',label:'Job ID'},{name:'p_expected_version',label:'Expected version',type:'number'}]} defaults={{p_job:id}}/><CommandForm command="job_action" fields={[{name:'p_job',label:'Job ID'},{name:'p_action',label:'Action',placeholder:'HOLD / RESUME / CANCEL'},{name:'p_reason',label:'Reason'},{name:'p_expected_version',label:'Expected version',type:'number'}]} defaults={{p_job:id}}/><CommandForm command="complete_job" fields={[{name:'p_job',label:'Job ID'},{name:'p_expected_version',label:'Expected version',type:'number'}]} defaults={{p_job:id}}/></div></main><ResourceList resource="visits"/></>}
