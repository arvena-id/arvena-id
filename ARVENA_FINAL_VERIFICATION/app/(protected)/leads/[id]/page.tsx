import { EntityDetail } from "@/components/entity-detail";import { CommandForm } from "@/components/command-form";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><EntityDetail entity="leads" id={id} editHref={`/leads/${id}/edit`}/><main className="page"><h2>Status action</h2><CommandForm command="transition_lead" fields={[{name:'p_lead',label:'Lead ID',required:true},{name:'p_to',label:'Target canonical state',required:true},{name:'p_reason',label:'Reason'},{name:'p_expected_version',label:'Expected version',type:'number',required:true}]} defaults={{p_lead:id}}/></main></>}
