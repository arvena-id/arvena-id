import { CommandForm } from "@/components/command-form";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <main className="page"><div className="page-head"><div><h1>Edit Draft Quote</h1><p>Issued Quote tidak dapat diedit.</p></div></div><CommandForm command="update_quote_draft" fields={[{name:'p_quote',label:'Quote ID'},{name:'p_expected_version',label:'Expected version',type:'number'},{name:'p_expires',label:'Expires',type:'datetime-local'},{name:'p_terms',label:'Terms'},{name:'p_document_discount',label:'Document discount minor',type:'number'},{name:'p_items',label:'Items JSON'}]} defaults={{p_quote:id}} onSuccess={`/quotes/${id}`}/></main>}
