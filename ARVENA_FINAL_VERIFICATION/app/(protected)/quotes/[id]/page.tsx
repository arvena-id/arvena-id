import { EntityDetail } from "@/components/entity-detail";import { CommandForm } from "@/components/command-form";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><EntityDetail entity="quotes" id={id} editHref={`/quotes/${id}/edit`}/><main className="page"><div className="grid-2"><CommandForm command="issue_quote" fields={[{name:'p_quote',label:'Quote ID'},{name:'p_expected_version',label:'Expected version',type:'number'}]} defaults={{p_quote:id}}/><CommandForm command="convert_quote_to_job" fields={[{name:'p_quote',label:'Accepted Quote ID'}]} defaults={{p_quote:id}}/></div></main></>}
