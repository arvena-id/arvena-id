import { CommandForm } from "@/components/command-form";import { getEntity } from "@/lib/data";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;const{row}=await getEntity('team',id);return <main className="page"><div className="page-head"><div><h1>Edit team member</h1><p>Disable/change role melalui command contract.</p></div></div><CommandForm command="update_member" fields={[{name:'p_member',label:'Member ID',required:true},{name:'p_role',label:'Role ID',required:true},{name:'p_status',label:'Status',required:true}]} defaults={{p_member:id,p_role:String(row?.role_id??''),p_status:String(row?.status??'ACTIVE')}} onSuccess={`/team/${id}`}/></main>}
