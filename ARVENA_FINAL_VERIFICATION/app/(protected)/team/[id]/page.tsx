import { EntityDetail } from "@/components/entity-detail";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <EntityDetail entity="team" id={id} editHref={`/team/${id}/edit`}/>}
