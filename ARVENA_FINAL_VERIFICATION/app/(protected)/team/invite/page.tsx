import { CommandForm } from "@/components/command-form";export default function Page(){return <main className="page"><div className="page-head"><div><h1>Invite team</h1><p>Invitation dibuat melalui server-authoritative RPC.</p></div></div><CommandForm command="invite_member" fields={[{name:'p_email',label:'Email',type:'email',required:true},{name:'p_role',label:'Role ID',required:true}]} onSuccess="/team"/></main>}
