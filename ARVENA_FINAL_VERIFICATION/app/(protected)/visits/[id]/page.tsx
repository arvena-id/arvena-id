import { EntityDetail } from "@/components/entity-detail";import { CommandForm } from "@/components/command-form";export default async function Page({params}:{params:Promise<{id:string}>}){const{id}=await params;return <><EntityDetail entity="visits" id={id}/><main className="page"><div className="page-head"><div><h2>Visit execution</h2><p>Depart → Arrive → Start → Finish. Evidence guards tetap server-authoritative.</p></div></div><CommandForm command="visit_action" fields={[{name:'p_visit',label:'Visit ID'},{name:'p_action',label:'Action',placeholder:'DEPART / ARRIVE / START / FINISH'},{name:'p_expected_version',label:'Expected version',type:'number'},{name:'p_reason',label:'Reason'}]} defaults={{p_visit:id}}/></main></>}
