import Link from "next/link";import { getSessionContext } from "@/lib/context";export default async function Page(){const{user}=await getSessionContext();return <main className="page"><div className="page-head"><div><h1>Mulai ARVENA</h1><p>{user?.email} · target: Job pertama dalam kurang dari 10 menit.</p></div></div><div className="panel stack"><p>1. Business → 2. Template → 3. Team → 4. Customer → 5. First Job</p><Link className="btn btn-primary" href="/onboarding/business">Mulai onboarding</Link></div></main>}
