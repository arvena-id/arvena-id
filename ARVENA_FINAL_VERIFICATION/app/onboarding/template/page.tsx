import Link from "next/link";import { getSessionContext } from "@/lib/context";export default async function Page(){const{activeOrganization}=await getSessionContext();return <main className="page"><div className="page-head"><div><h1>Template</h1><p>{activeOrganization?.name}. Template industry tetap konfigurasi, bukan schema baru.</p></div></div><div className="panel"><p>Default generic workflow aktif. Template khusus dapat dipilih dari data template yang tersedia saat backend chain tervalidasi.</p><Link className="btn btn-primary" href="/onboarding/team">Lanjut</Link></div></main>}
