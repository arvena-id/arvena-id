"use client";import { FormEvent,useState } from "react";
export function OnboardingBusiness(){const[busy,setBusy]=useState(false);const[msg,setMsg]=useState<string|null>(null);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setMsg(null);const f=new FormData(e.currentTarget);const body=Object.fromEntries(f);const r=await fetch('/api/onboarding/organization',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(body)});const j=await r.json();setBusy(false);if(!r.ok){setMsg(j.error??'Belum berhasil');return;}location.assign('/onboarding/template')};return <form className="panel stack" onSubmit={submit}><label className="field"><span className="label">Nama bisnis</span><input className="input" name="name" required/></label><label className="field"><span className="label">Slug</span><input className="input" name="slug" required/></label><div className="grid-2"><label className="field"><span className="label">Country</span><input className="input" name="country_code" defaultValue="ID" maxLength={2}/></label><label className="field"><span className="label">Currency</span><input className="input" name="currency_code" defaultValue="IDR" maxLength={3}/></label></div><label className="field"><span className="label">Timezone</span><input className="input" name="timezone" defaultValue="Asia/Jakarta"/></label><input type="hidden" name="locale" value="id-ID"/>{msg&&<div className="error-state">{msg}</div>}<button className="btn btn-primary" disabled={busy}>{busy?'Membuat…':'Buat organization'}</button></form>}
