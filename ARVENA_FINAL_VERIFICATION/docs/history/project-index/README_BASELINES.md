# ARVENA Implementation Baselines

The files in `archive/` are historical snapshots and are not the current source repository.

- `ARVENA_P0_BASELINE_AFTER_INITIAL_BUILD.tar.gz` — first historical baseline archive.
- `ARVENA_P0_BASELINE_AFTER_INITIAL_BUILD_v2.tar.gz` — later historical baseline archive used in subsequent work before the current filesystem state.

Canonical current source is:

`../05_CURRENT_REPOSITORY/arvena-p0/`

Do not use either archived baseline as the current implementation unless an explicit rollback/recovery decision is made.
